<!DOCTYPE html>
<html>
  <head>
    <title>Book Details</title>
    <style>
      body {
        font-family: Arial;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .book-details {
        background: #f9f9f9;
        padding: 20px;
        border-radius: 5px;
      }
      .back-link {
        display: inline-block;
        margin-top: 20px;
        padding: 8px 16px;
        background: #2196f3;
        color: white;
        text-decoration: none;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <h1>Book Details</h1>
    <div class="book-details">
      <p><strong>ID:</strong> <%= book.id %></p>
      <p>
        <strong>Author:</strong> <%= book.author_first_name %> <%=
        book.author_last_name %>
      </p>
      <p><strong>Title:</strong> <%= book.title %></p>
      <p><strong>Added On:</strong> <%= book.created_at.toLocaleString() %></p>
    </div>
    <a href="/" class="back-link">← Back to All Books</a>
  </body>
</html>
